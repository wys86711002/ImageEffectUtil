<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.wuyongsen.imageeffectutils">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ImageEffectActivity">
        </activity>
        <activity android:name=".BitmapPiexlActivity">
        </activity>
        <activity android:name=".CircleImageViewActivity">
        </activity>
        <activity android:name=".ReflectionViewActivity">
        </activity>
        <activity
            android:name=".ScaleImageViewActivity"
            android:label="@string/title_activity_scale_image_view"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".DrawMeshActivity">
        </activity>
    </application>

</manifest>